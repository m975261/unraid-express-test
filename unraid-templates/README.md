# Unraid Templates for express-test

XML templates for Unraid Community Applications.

## Install
Copy `my-express-test.xml` to `/boot/config/plugins/dockerMan/templates-user/` on Unraid,
then: Docker tab → Add Container → select from template list.
